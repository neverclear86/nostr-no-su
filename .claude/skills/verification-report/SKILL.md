---
name: verification-report
description: nostr-no-su の main を docker compose 上で実機検証し、issue #16 と #45 の形式の動作確認レポート（スクリーンショット付き）を GitHub issue に投稿する手順。「動作確認レポート」「実機検証」「#16 のように確認して」と頼まれたときに使う。
---

# 動作確認レポート（nostr-no-su）

対象のリポジトリは `neverclear86/nostr-no-su`（private）である。
レポートの手本は #16 と #45 で、どちらも issue 本文に概要を、最初のコメントに詳細レポートを置いている。
作業を始める前に両方を読み、構成を合わせる。

```sh
gh issue view 45 -R neverclear86/nostr-no-su
gh api repos/neverclear86/nostr-no-su/issues/45/comments --jq '.[0].body'
```

コマンドの具体例は同じディレクトリの [reference.md](reference.md) にまとめてある。

## 守ること

- **スクリーンショットの貼り方**：`gh` の `--attach` で添付する（gh 2.100 以上）。画像を置くためのブランチやリポジトリ内のファイルは作らない。ブラウザーからのアップロードもしない
- **ユーザーの環境**：ユーザーが動かしている compose スタック（コンテナー名 `nostr-no-su-*`、127.0.0.1:8080）と作業ツリーは使わず、止めもしない。検証はスクラッチパッドに `git worktree add` した main から、プロジェクト名 `nns-verify` と空いているポートで行う
- **鍵とパスワード**：使い捨ての秘密鍵、マスターキー、管理パスワードだけを使い、スクラッチパッドに置く。レポートにも使い捨てであることを書く
- **文体**：issue とコメントは標準的な技術文体の日本語で書く。書く前に `japanese-tech-writing` スキルを読む
- **不具合の扱い**：コードは直さない。見つけた不具合は別 issue として起票し、レポートから参照する（#16 と #17、#45 と #46 の前例）
- **エージェント**：作業をエージェントに任せるときは opus を使う

## 手順

### 1. 対象を決める

`git log --oneline -1 origin/main` で対象のコミットを決める。
README、`docs/architecture.md`、直近の issue と PR を読み、前回のレポート以降に増えた機能を確認項目に足す。

### 2. 環境を立てる

compose は worktree の `docker-compose.yml` に、strfry 2 台を足す override ファイルを重ねて起動する。
環境変数は `--env-file` でスクラッチパッドから渡し、worktree には置かない。
毎回同じ引数になるので、ラッパーのスクリプトにしておく。

- strfry のイメージは既定の設定で 127.0.0.1 に bind するので、そのままでは他のコンテナーから届かない。`bind = "0.0.0.0"` に書き換えた設定ファイルをマウントする
- `ADMIN_PORT` はアプリの待ち受けとホストへの公開の両方に効く。`ADMIN_BASE_URL` も同じポートで設定すると、`auth_url` をホストのブラウザーでそのまま開ける
- `event_logger` プラグインは本体と同じ gleam イメージの中でビルドし、worktree の `plugins/event_logger` に出力する。出力は root 所有になるので、`chmod -R a+rX` もコンテナーの中で行う
- 起動の順序は、Postgres と strfry、プラグインのビルド、本体のイメージのビルド、本体の順にする

### 3. 検証する

各項目で実際の出力（ログ、状態コード、クライアントの出力）を保存し、レポートにはそこから抜粋する。
compose を env を変えて作り直すと前のコンテナーのログが消えるので、作り直す前に `logs` を保存する。

1. **起動**：healthy、非 root、再起動回数 0、`schema ready` と `loaded N account(s)`。マスターキーが無いときと不正なときに、理由がログとダッシュボードに出ること。`ACCOUNT_KEYS` と `BUNKER_SECRET` は compose の `environment:` に無く env-file では届かないので、`compose run -e` で渡して名前だけが報告されることを確かめる
2. **管理 UI のアカウント管理**：Playwright で実際のブラウザーを操作し、状態コードとヘッダーは curl でも確かめる。nsec 入力による登録、重複（409）と不正な nsec（400、入力値を表示しない）、鍵の生成（確認ページだけに nsec、登録は 303）、生成した鍵のラベル不正（同じ nsec の確認ページを再表示）、ラベルの編集（絵文字を含む）、コピーのボタン、秘密鍵の再表示（誤ったパスワードで 403、正しいパスワードで 200 とログ 1 行）、削除（再送は 404）、secret のローテーション
3. **NIP-46 とバンカー経由の投稿**：UI からコピーした URI で、connect、get_public_key、sign_event、ping、nip44_encrypt、nip44_decrypt、logout を実行する。kind 1 を署名して 2 台のリレーへ発行し、読み戻して署名を検証する
4. **再起動なしのアカウント変更**：クライアントを接続したまま UI でアカウントを追加し、すぐ署名できること、削除すると応答しなくなること、既存のセッションが続くことを確かめる
5. **ローテーション**：古い URI からの新しい connect は承認待ち（`auth_url`）になり、接続済みのセッションは続く。残った承認待ちは拒否して片付ける
6. **永続化**：本体のコンテナーを再起動しても同じ URI で署名できること。`pg_dump` に平文の鍵も secret も無いこと
7. **DB の停止と復帰**：`docker pause` で止め、署名と監視が続くこと、最初の変更が 202 になり以後の操作が 503 になること、ログが停止と復帰で 1 行ずつであることを確かめる
8. **マルチリレーと重複排除**：2 台から同じイベントが届いても `events` は 1 行で、kind 24133 は保存されないこと
9. **auth_url と取り消し**：secret 無しの URI で接続して承認と拒否を行い、承認済みセッションを取り消す
10. **管理 UI の保護**：未認証の 401、`/healthz`、未知のパスの 404、誤ったメソッドの 405、別オリジンの POST の 400、アカウントのページの `cache-control: no-store` と枠への埋め込みの禁止
11. **リレー**：`docker restart` で片方を再起動して再接続と購読の再開を確かめ、`docker stop` で片方を止めた状態で署名と投稿が成立することを確かめる
12. **スーパービジョン**：コンテナーの BEAM には入れないので、worktree で本体を分散ノードとして `gleam run` し、別ノードから `rpc:call` でバンカーのアクターを kill する。同じ名前で再起動し、DB からアカウントを読み直して署名が続くことを確かめる
13. **署名者 0 件**：アカウントをすべて削除すると購読が閉じ、追加すると戻ることを確かめる。アカウントが無くなるので、撮影をすべて終えてから最後に行う
14. **秘密の grep**：全ログ、`pg_dump`、鍵を表示するページ以外の応答本文を、秘密鍵（16 進と nsec）、secret、マスターキー、管理パスワード、DB のパスワードで検索し、一致が 0 件であることを確かめる

### 4. スクリーンショットを撮る

Playwright で幅 1280、ページ全体を撮る。
ファイル名の番号はレポート本文での並び順にする。
同じ名前で撮り直すと前の画面が消えるので、再実行のときは撮らないか名前を変える。
承認済みセッションはクライアントを終了しても残るので、ダッシュボードの撮影はセッションが増える前に行うか、不要なセッションを取り消してから行う。

標準のセットは次のとおりである。

| ファイル | 画面 |
| --- | --- |
| `01-disabled-nokey.png` | マスターキーが無いときのダッシュボード |
| `02-disabled-badkey.png` | マスターキーが不正なときのダッシュボード |
| `03-dashboard-empty.png` | アカウント 0 件のダッシュボード |
| `04-dashboard-accounts.png` | アカウントを登録した後のダッシュボード |
| `05-new-account.png` | アカウントの登録画面 |
| `06-registered.png` | nsec 入力による登録の完了ページ |
| `07-generated.png` | 生成した鍵の確認ページ |
| `08-generated-invalid-label.png` | ラベルが不正なときの生成鍵の確認ページ |
| `09-duplicate.png` | 登録済みの nsec の 409 |
| `10-invalid-nsec.png` | 不正な nsec の 400 |
| `11-edit-label.png` | ラベルの編集ページ |
| `12-reveal-form.png` | 秘密鍵の再表示のパスワード入力 |
| `13-reveal-wrong.png` | パスワードが違うときの 403 |
| `14-reveal-result.png` | 秘密鍵の再表示 |
| `15-delete-confirm.png` | 削除の確認ページ |
| `16-rotate-confirm.png` | secret のローテーションの確認ページ |
| `17-change-not-confirmed.png` | DB の停止中の変更の 202 |
| `18-dashboard-db-down.png` | DB の停止中のダッシュボード |
| `19-accounts-unavailable.png` | DB の停止中の操作ページの 503 |
| `20-pending.png` | 承認待ちのあるダッシュボード |
| `21-approve-page.png` | 接続の承認ページ |
| `22-approved.png` | 承認後の画面 |
| `23-denied.png` | 拒否後の画面 |
| `24-dashboard-sessions.png` | 承認済みセッション、リレー、プラグインを含むダッシュボード |
| `25-relay-down.png` | リレーを 1 台止めている間のダッシュボード |

UI に画面が増えたときは、この表にも足す。

### 5. 投稿する

1. レポートの issue を作る。本文は目的、検証環境、確認項目（結果に合わせてチェックする）、結果の順に書く
2. 不具合があれば別 issue として起票する。レポートの issue と不具合の issue は互いに番号を参照するので、先にレポートの issue を仮の文字列入りで作り、不具合の issue を作ってから `gh issue edit` で差し替える
3. 詳細レポートをコメントとして投稿する。画像は本文で `![代替テキスト](./04-dashboard-accounts.png)` のようにローカルのパスで参照し、スクリーンショットのディレクトリで同じパスを `--attach` に渡す。gh が参照を `https://github.com/user-attachments/assets/...` に書き換える
4. 投稿後に本文を取得し、`./` の参照が残っていないこと、画像の数が添付した数と一致すること、各 URL が認証付きで `200 image/png` を返すことを確かめる

詳細レポートの構成は #45 に合わせる。
対象コミット、検証環境の表、結果サマリーの表、領域ごとの節（ログの抜粋と、UI の節にはスクリーンショット）、気づいた点、後片付けの順である。

### 6. 後片付け

- 本体のローカル実行を止めてから、テスト用の Postgres を消す（逆の順序だと pgo のクラッシュレポートに DB のパスワードが出る）
- `nns-verify` のスタックを volume とビルドしたイメージごと消し、ローカル実行用のコンテナーとバックグラウンドのプロセスも止める
- worktree の root 所有のファイルをコンテナー経由で消してから、`git worktree remove --force` で worktree を消す
- スクラッチパッドの鍵、secret、env ファイル、`pg_dump` を消す。投稿に使ったスクリーンショットも、写っている鍵が使い捨てでも残さない
- ユーザーの `nostr-no-su-*` コンテナーが再起動されずに動いていることを確かめる

## つまずきやすい点

- **秘密の grep の準備**：ローテーションや削除の後では元の secret を取り出せない。変更の前に毎回、ダッシュボードの URI を保存しておく。記録漏れの保険として、ログに 32 文字の 16 進の語が無いことも確かめる（64 文字の 16 進は公開鍵とイベント id で埋まるので、一覧を見ても役に立たない）
- **取り消しの対象**：同じ署名者に複数のセッションがあるので、署名者だけで取り消しのフォームを選ぶと別のセッションを消す。クライアントの公開鍵でも絞り、一致が 1 件であることを確かめてから押す。ダッシュボードの HTML は 1 行なので、`sed` の行範囲で節を切り出す方法は使えない
- **DB の止め方**：`docker pause` では接続が切れないので、pgo のクラッシュレポートは出ない。`docker stop` や `docker rm` で接続が切れると、クラッシュレポートに DB のパスワードが出る（README に書いてある既知の制約）
- **名前解決の失敗**：`docker stop` でリレーを止めると compose の DNS から名前が消え、再接続のたびにクラッシュレポートが出る（#46）。修正されるまでは既知の件として扱い、`docker restart` では出ないことと区別して書く
- **再起動後のログ**：本体やアクターの再起動の直後に出る `ignored: request predates this bunker instance` は、再起動前のリクエストを再実行しないための保護で、正常な動作である
- **プロセスの停止**：`pkill -f 'nnsverify'` はそのコマンドを実行しているシェル自身にも一致して、シェルごと終了する。`pkill -f '[b]eam.smp.*nnsverify'` のように、自分に一致しないパターンにする
- **FIFO のクライアント**：接続したままのクライアントに命令を送るとき、FIFO を `exec 3<>fifo` のように読み書き両方で開いておかないと、書き込み側が止まる
- **nostr-tools の拒否**：`BunkerSigner` は拒否を `Error` ではなく文字列で投げる。表示は `e instanceof Error ? e.message : String(e)` にする
- **本文ファイル**：エージェントの環境によってはレポートのファイルを Write で作れないことがある。その場合は `--body-file -` にヒアドキュメントで渡す
