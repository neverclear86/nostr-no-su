# 変更履歴

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 1.1.0 に従う。版の付け方と書き方の決まりは [CONTRIBUTING.md](CONTRIBUTING.md) の「版数」「変更履歴」を参照。

## [Unreleased]

### 追加

- 公開イメージで動かすディレクトリーを 1 行のコマンドで作る `install.sh`。最新の Release の版を調べ、`.env` の `NOSTR_NO_SU_VERSION` をその版に固定する（#693）

### 変更

- **破壊的変更**: アプリと docker イメージの既定の待ち受けポート（`ADMIN_PORT` 未設定のときの値）を 8080 から 24133 にした（#701）
- **破壊的変更**: 同梱の compose で `.env` の `ADMIN_PORT` をホスト側に公開するポート（既定 24133）の意味にし、コンテナーには渡さず内側を 24133 に固定した。`ADMIN_PORT=`（空）は管理 UI を無効にせず既定の 24133 で公開する（無効にするのは override の `ADMIN_PORT: ""`）。`ADMIN_BASE_URL` の既定はホスト側の公開ポートを指す（#702）
- `relays` テーブルに登録したリレーの URL が `ws://`・`wss://` 以外のスキーム（`http://`、`https://` など）のとき、接続せずに起動時の `skipped registered relay` の Warning で飛ばすようにした（#803）
- `relays` テーブルの `observe` と `bunker` がどちらも false の行を、起動時の `skipped registered relay: no role` の Warning で飛ばすのをやめ、読み込まずダッシュボードの一覧にも出さないようにした。消し方は [運用](docs/operations.md)（#805）
- 管理 UI からの承認・拒否・セッションの取り消し・権限の編集・`nostrconnect://` の接続の成功のログ行を、ストアへの書き込みが成功した後にバンカーのアクターが出すようにし、接頭辞を `[admin]` から `[bunker]` にした（本文は同じ）。読み込み前の `nostrconnect://` の接続の失敗も行を出す（#799）
- 管理 UI に出るプラグインの無効の理由と読み込めなかったプラグインの理由、プラグインの実行の失敗のログ行で、改行と制御文字を空白にし、長さの上限（理由 120、スタックトレース 400）を書記素ではなくコードポイントで数えるようにした（#897）
- 管理 UI のプラグインの再有効化の失敗（名前に一致するプラグインが無い 404、ランナーが応答しない 503）の本文を、英語のままではなく表示の言語で出すようにした（#836）

### 修正

- 平文 HTTP の非 localhost で開いた管理 UI のコピーのボタンがクリップボードへ書けず、選択と手動コピーの案内だけになっていたのを、`document.execCommand("copy")` で書くようにした（#697）
- 同梱の `event_logger` で監視対象の保存が途中で失敗すると行が 0 件のまま残り、保存の対象が全アカウントに広がっていたのを、入れ替えを 1 文の SQL にして失敗したときは元の対象のまま残るようにした（#834）

## [0.1.0] - 2026-09-24

最初のリリース。0.1.0 より前の変更は個別には記録せず、機能ごとにまとめて主な issue と PR の番号を添える。

### 追加

- NIP-46 リモート署名バンカー。`connect` / `get_public_key` / `sign_event` / `ping` / `nip44_encrypt` / `nip44_decrypt` / `logout` に応え、複数アカウントの鍵を預かり、複数のリレーに専用の接続を張る（#6 #155）
- 接続の権限。`connect` で宣言した権限（perms）の範囲だけ署名と暗号化を許し、perms を宣言しないセッションには kind 24133 を除く署名と NIP-44 の暗号化・復号を既定で許す。承認済みセッションの権限は管理 UI から編集できる（#271 #428 #430）
- secret を持たないクライアントの管理 UI での承認（`auth_url` フロー）と、承認待ちと承認済みセッションの件数の上限（#12 #259 #265 #550）
- `nostrconnect://` の URI による接続。URI のリレーと権限を並べる確認のダイアログを挟み、URI のリレーはリレーの一覧に登録せず、セッションだけの接続として開閉する（#299 #332 #334 #644 #646 #651 #653 #686）
- リレーの AUTH（NIP-42）への登録アカウントごとの鍵での応答と、セッションの外のリクエストの pubkey ごとと全体の上限（#210 #553 #572）
- BIP-340 Schnorr 署名と NIP-44 v2 暗号化（拡張長さプレフィックスを含む）の自前実装（公式のテストベクターに一致）（#131 #339）
- 登録アカウントのイベント監視（複数リレー、ID と署名の検証、リレー横断の重複排除、保存した再開点からの購読、CLOSED の理由に応じた購読の張り直し）（#130 #176 #200 #555）
- プラグイン機構（プラグイン API v1、`PLUGIN_DIR` からの読み込み、専用プロセスによる障害隔離、本体の `DATABASE_URL` の予約キー `DatabaseUrl` での受け渡し、本体の版の下限の宣言 `plugin_min_host_version/0`、読み込めなかったプラグインの理由つきの表示）（#19 #471 #472 #681）
- プラグインのページ（管理 UI に描く記述、フォームと実行、image の部品、表示の言語）と、登録アカウントの名義でのイベントの送信と取得の口（#427 #456 #461 #634 #459 #462 #574）
- 同梱プラグイン `event_logger`（受信したイベントを本体と同じ Postgres の `event_logger_events` テーブルへ保存し、監視対象の設定とタイムラインのページを持つ）と内蔵プラグイン `console_logger`（#34 #431 #433 #649 #678 #681）
- 同梱プラグイン `profile`（登録アカウントのプロフィール（kind 0）の表示と、フォームからの編集と送信）（#464 #465 #637）
- 管理 UI（サーバー側描画、日本語と英語、ライトとダーク）。アカウントの登録・生成・削除・secret の再生成・ラベルの編集・秘密鍵の再表示・DB からの読み直し、接続の承認と拒否、セッションの取り消し、リレーの追加・用途の編集・削除、プラグインの状態と再有効化（#29 #36 #174 #333）
- 管理 UI のデザイン（ロゴとブランドの配色、概要の帯、公開鍵の指紋、状態の色、kind 0 の picture によるアカウントのアイコン）。モックのデザインで作り直した（#434 #337 #509 #683）
- 管理 UI の操作はページを持たずダッシュボードのダイアログで行い、節の説明は ⓘ の補足にまとめた（#669 #679 #680 #685 #686）
- 接続 QR コードのダイアログ（`bunker://` と `nostrconnect://` のタブ、カメラでコピーできる形）（#457 #463 #625 #685）
- アカウント（秘密鍵と接続 secret は `ACCOUNT_MASTER_KEY` で AES-256-GCM により暗号化）、リレー、セッション、承認待ち、再開点の Postgres への保存と、版つきの前向きの移行。セッションと承認待ちの行には HMAC を付け、合わない行を読み込みで捨てる（#42 #148 #195 #240 #583）
- docker compose（Postgres 同梱、読み取り専用のルート、非 root、healthcheck、remsh の口、秘密のファイルからの読み込み）、公開イメージ `ghcr.io/neverclear86/nostr-no-su`（`linux/amd64` と `linux/arm64`）、公開イメージから起動する `docker-compose.release.yml`、`.env` を用意する `setup-env.sh`（#162 #203 #219 #390 #402 #408 #561）
- 文書: 使い方、設定、運用（バックアップ、更新、復旧、マスターキーの交換）、管理 UI、プラグイン API v1、設計上の判断と既知の制約、システム構成、開発、貢献の手引き、脆弱性の報告の窓口（#413 #446 #664）

[Unreleased]: https://github.com/neverclear86/nostr-no-su/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/neverclear86/nostr-no-su/releases/tag/v0.1.0
